# My dotfiles

The directory `dotfiles` contains my dotfiles. The script `getfiles` is used to retrieve my dotfiles and put them in this repo. The script `putfiles` does the opposite. I don't recommend using the latter, since it is hardly usable and often unstable.
